# Structured Decoding — Ontology Worlds
# ----------------------------------------
# No anthropic SDK needed — runs inside Claude Code (the LLM).

# Optionnel : structured decoding réel avec logits (modèle local requis)
# outlines>=0.1.0          # https://github.com/dottxt-ai/outlines
# guidance>=0.1.0          # https://github.com/guidance-ai/guidance
# transformers>=4.40.0     # Pour les modèles HuggingFace locaux
# torch>=2.0.0             # Backend PyTorch pour Outlines

# Ontology builder (OKG + Wikidata API calls)
httpx>=0.27.0

# Utilitaires
python-dotenv>=1.0.0       # Chargement des variables d'environnement
jsonschema>=4.20.0         # Validation JSON Schema

# Apps (consommation + construction)
streamlit>=1.30.0
duckdb>=0.9.0
pandas>=2.0.0
